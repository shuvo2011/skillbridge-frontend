import ContactClient from "@/components/module/contact/contact-client";

export const metadata = {
	title: "Contact Us - SkillBridge",
	description: "Get in touch with the SkillBridge team. We're here to help with any questions or concerns.",
};

export default function ContactPage() {
	return <ContactClient></ContactClient>;
}
